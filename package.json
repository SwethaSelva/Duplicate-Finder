{
  "name": "eliminate_duplicates",
  "version": "1.0.0",
  "description": "This will find the duplicate files by it's name",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Swetha Selvakumaran",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
}
